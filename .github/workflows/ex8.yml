# 工作流名称
name: Ex8 - Workflow Programmable (Cron Job)

# on: 定义触发条件
on:
  push:
    branches:
      - ex8
  schedule:
    # 每天早上 8 点执行一次（UTC 时间）
    # 注意：GitHub 使用的是 UTC 时间，如果你在法国，
    # 法国冬令时 = UTC+1，夏令时 = UTC+2，
    # 所以实际执行时间会是法国时间 9h 或 10h。
    - cron: '0 8 * * *'

jobs:
  # Q1：唯一的任务 job，用于显示计划任务信息
  log-message:
    runs-on: ubuntu-latest  # 使用 GitHub 提供的 Ubuntu 运行环境

    steps:
      # 显示一条日志消息
      - name: Afficher un message dans les logs  # 显示日志信息
        run: echo "🕗 Le workflow s'exécute automatiquement chaque jour à 8h (heure UTC)."
